import { OtherDirective } from './other.directive';

describe('OtherDirective', () => {
  it('should create an instance', () => {
    const directive = new OtherDirective();
    expect(directive).toBeTruthy();
  });
});

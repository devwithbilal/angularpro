import { HiddingDirective } from './hidding.directive';

describe('HiddingDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddingDirective();
    expect(directive).toBeTruthy();
  });
});
